@page "/running-plan/{TargetUserId:guid}/{NombreAtleta}"
@using CrossBoxApp.Models
@using CrossBoxApp.Models.Services
@inject NavigationManager Nav
@inject HttpClient Http

<div class="top-navbar">
    <div class="nav-left" @onclick="Volver"><i class="fas fa-arrow-left"></i> VOLVER</div>
    <div class="nav-title">PLAN: @NombreAtleta.ToUpper()</div>
    <div class="nav-right"></div>
</div>

<div class="running-container">

    <div class="month-control">
        <button class="btn-nav" @onclick="MesAnterior"><i class="fas fa-chevron-left"></i></button>
        <span class="month-label">@fechaActual.ToString("MMMM yyyy").ToUpper()</span>
        <button class="btn-nav" @onclick="MesSiguiente"><i class="fas fa-chevron-right"></i></button>
    </div>

    <div class="calendar-grid">
        <div class="day-name">DOM</div> <div class="day-name">LUN</div> <div class="day-name">MAR</div>
        <div class="day-name">MIE</div> <div class="day-name">JUE</div> <div class="day-name">VIE</div>
        <div class="day-name">SAB</div>

        @for (int i = 0; i < diasVaciosInicio; i++)
        {
            <div class="calendar-day empty"></div>
        }

        @for (int dia = 1; dia <= diasEnMes; dia++)
        {
            int diaActual = dia;
            var log = ObtenerLog(diaActual);
            bool tieneData = log != null;
            bool estaRealizado = tieneData && (log.Realizado == true);

            string colorBorde = tieneData ? ObtenerColor(log.Tipo) : "#333";

            <div class="calendar-day"
                 style="border-color: @colorBorde; background: @(tieneData ? "rgba(30,30,30,0.8)" : "#1a1a1a")"
                 @onclick="() => AbrirModal(diaActual)">

                <span class="day-number" style="color: @(tieneData? colorBorde : "#666")">@dia</span>

                @if (tieneData)
                {
                    <div class="day-content">
                        <i class="fas fa-running running-icon" style="color: @colorBorde"></i>
                        <span class="day-type" style="color: @colorBorde">@log.Tipo</span>
                    </div>

                    // --- INDICADOR DE QUE YA CUMPLIÓ ---
                    @if (estaRealizado)
                    {
                        <div class="done-badge"><i class="fas fa-check"></i></div>
                    }
                }
            </div>
        }
    </div>

    <div class="legend-scroll">
        @foreach (var t in TiposEntreno)
        {
            <div class="legend-item"><span class="dot" style="background:@t.Color"></span> @t.Nombre</div>
        }
    </div>
</div>

@if (mostrarModal)
{
    <div class="modal-overlay">
        <div class="modal-content">
            <div class="modal-header">
                <h5>@fechaSeleccionada.ToString("dddd dd 'de' MMMM")</h5>
                <button class="btn-close-custom" @onclick="CerrarModal"><i class="fas fa-times"></i></button>
            </div>

            <div class="modal-body">

                @if (logEditando.Realizado == true)
                {
                    <div class="feedback-section">
                        <div class="feedback-header">
                            <i class="fas fa-check-circle"></i> ENTRENAMIENTO COMPLETADO
                        </div>

                        <div class="feedback-row">
                            <div class="rpe-display" style="background: @ObtenerColorRPE(logEditando.RPE ?? 0)">
                                RPE: @(logEditando.RPE ?? 0)/10
                            </div>
                        </div>

                        <label class="feedback-label">Comentarios del Atleta:</label>
                        <div class="feedback-text">
                            @(string.IsNullOrEmpty(logEditando.ComentariosAtleta) ? "Sin comentarios..." : logEditando.ComentariosAtleta)
                        </div>
                    </div>
                    <hr class="divider" />
                }

                <label>Tipo de Sesión:</label>
                <div class="type-grid">
                    @foreach (var tipo in TiposEntreno)
                    {
                        <div class="type-btn @(logEditando.Tipo == tipo.Nombre ? "selected" : "")"
                             style="border-color: @tipo.Color; color: @(logEditando.Tipo == tipo.Nombre ? "black" : "white"); background: @(logEditando.Tipo == tipo.Nombre ? tipo.Color : "transparent")"
                             @onclick="() => SeleccionarTipo(tipo.Nombre)">
                            @tipo.Nombre
                        </div>
                    }
                </div>

                <label class="mt-3">Instrucciones Breves (Título):</label>
                <input @bind="logEditando.Titulo" class="form-control input-dark mb-2" placeholder="Ej: 12k Ritmo Carrera" />

                <label>Detalles / Instrucciones Completas:</label>
                <textarea @bind="logEditando.Descripcion" class="form-control input-dark" rows="4" placeholder="Desglosa el calentamiento, bloques principales..."></textarea>
            </div>

            <div class="modal-footer">
                @if (logEditando.RunningLogID != Guid.Empty)
                {
                    <button class="btn-delete" @onclick="EliminarLog"><i class="fas fa-trash"></i></button>
                }
                <button class="btn-confirm" @onclick="GuardarLog">GUARDAR CAMBIOS</button>
            </div>
        </div>
    </div>
}

<style>
    /* Estilos Base */
    .top-navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 15px;
        background: #1a1a1a;
        border-bottom: 2px solid #F8C545;
        color: white;
        font-family: 'Oswald';
    }

    .nav-left {
        color: #F8C545;
        cursor: pointer;
    }

    .nav-title {
        font-size: 1.1rem;
        letter-spacing: 1px;
    }

    .running-container {
        padding: 15px;
        background: #0d0d0d;
        min-height: 100vh;
        color: white;
    }

    /* Calendario */
    .month-control {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 15px;
        background: #1a1a1a;
        padding: 10px;
        border-radius: 8px;
        border: 1px solid #333;
    }

    .month-label {
        font-family: 'Oswald';
        font-size: 1.3rem;
        color: #F8C545;
    }

    .btn-nav {
        background: none;
        border: none;
        color: white;
        font-size: 1.5rem;
        width: 40px;
    }

    .calendar-grid {
        display: grid;
        grid-template-columns: repeat(7, 1fr);
        gap: 6px;
    }

    .day-name {
        text-align: center;
        color: #666;
        font-size: 0.7rem;
        font-weight: bold;
        margin-bottom: 5px;
    }

    .calendar-day {
        background: #1a1a1a;
        border: 1px solid #333;
        border-radius: 6px;
        min-height: 75px;
        padding: 4px;
        position: relative;
        cursor: pointer;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

        .calendar-day.empty {
            background: transparent;
            border: none;
            cursor: default;
        }

    .day-number {
        font-size: 0.8rem;
        font-weight: bold;
        align-self: flex-end;
    }

    .day-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100%;
        margin-top: -15px;
    }

    .running-icon {
        font-size: 1.5rem;
        filter: drop-shadow(0 0 2px rgba(0,0,0,0.5));
    }

    .day-title {
        font-size: 0.55rem;
        text-transform: uppercase;
        font-weight: bold;
        margin-top: 2px;
        text-align: center;
        line-height: 1;
        text-shadow: 1px 1px 1px black;
    }

    /* INDICADOR DE COMPLETADO (NUEVO) */
    .done-badge {
        position: absolute;
        top: -5px;
        left: -5px;
        background: #28a745;
        color: white;
        width: 20px;
        height: 20px;
        border-radius: 50%;
        font-size: 0.7rem;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 2px 5px rgba(0,0,0,0.5);
        border: 2px solid #1a1a1a;
    }

    /* Modal Tipos */
    .type-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 8px;
    }

    .type-btn {
        border: 1px solid;
        padding: 8px 2px;
        text-align: center;
        border-radius: 5px;
        font-size: 0.7rem;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.2s;
        text-transform: uppercase;
    }

        .type-btn:active {
            transform: scale(0.95);
        }

    .legend-scroll {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-top: 20px;
        justify-content: center;
    }

    .legend-item {
        font-size: 0.7rem;
        color: #ccc;
        display: flex;
        align-items: center;
        gap: 5px;
    }

    .dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        display: inline-block;
    }

    /* Modal General */
    .modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.9);
        z-index: 3000;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .modal-content {
        background: #1a1a1a;
        width: 90%;
        border-radius: 12px;
        border: 1px solid #F8C545;
        padding: 0;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        max-height: 90vh;
    }

    .modal-header {
        background: #F8C545;
        padding: 10px 15px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        color: black;
        font-family: 'Oswald';
    }

    .modal-body {
        padding: 15px;
        color: white;
        overflow-y: auto;
    }

    .modal-footer {
        padding: 10px 15px;
        display: flex;
        justify-content: space-between;
        gap: 10px;
    }

    /* ESTILOS FEEDBACK (NUEVO) */
    .feedback-section {
        background: rgba(40, 167, 69, 0.1);
        border: 1px solid #28a745;
        padding: 15px;
        border-radius: 10px;
        margin-bottom: 20px;
    }

    .feedback-header {
        color: #28a745;
        font-weight: bold;
        font-size: 1rem;
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 10px;
    }

    .feedback-text {
        font-style: italic;
        color: #ddd;
        background: rgba(0,0,0,0.3);
        padding: 10px;
        border-radius: 5px;
        font-size: 0.9rem;
    }

    .feedback-label {
        font-size: 0.8rem;
        color: #888;
        margin-top: 10px;
        display: block;
    }

    .rpe-display {
        display: inline-block;
        padding: 5px 15px;
        border-radius: 20px;
        color: black;
        font-weight: bold;
        font-family: 'Oswald';
        margin-bottom: 5px;
        box-shadow: 0 0 10px rgba(0,0,0,0.5);
    }

    .divider {
        border-top: 1px dashed #555;
        margin: 20px 0;
    }

    .input-dark {
        background: #333 !important;
        color: white !important;
        border: 1px solid #666;
        font-size: 0.9rem;
    }

    .btn-confirm {
        background: #F8C545;
        color: black;
        border: none;
        padding: 10px;
        font-weight: bold;
        flex: 1;
        border-radius: 5px;
    }

    .btn-delete {
        background: #d9534f;
        color: white;
        border: none;
        padding: 10px;
        border-radius: 5px;
        width: 45px;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .btn-close-custom {
        background: none;
        border: none;
        font-size: 1.2rem;
    }
</style>

@code {
    [Parameter] public Guid TargetUserId { get; set; }
    [Parameter] public string NombreAtleta { get; set; }

    private DateTime fechaActual = DateTime.Today;
    private List<RunningLogs> logsDelMes = new List<RunningLogs>();
    private int diasEnMes;
    private int diasVaciosInicio;

    // Modal
    private bool mostrarModal = false;
    private RunningLogs logEditando = new RunningLogs();
    private DateTime fechaSeleccionada;

    // DEFINICIÓN DE TIPOS Y COLORES
    class TipoEntreno { public string Nombre { get; set; } public string Color { get; set; } }

    private List<TipoEntreno> TiposEntreno = new List<TipoEntreno>
    {
        new TipoEntreno { Nombre = "Acumulativo", Color = "#00BFFF" },
        new TipoEntreno { Nombre = "Intensidad", Color = "#FF4500" },
        new TipoEntreno { Nombre = "Tempo", Color = "#FF8C00" },
        new TipoEntreno { Nombre = "Cuestas", Color = "#9370DB" },
        new TipoEntreno { Nombre = "Fuerza", Color = "#808080" },
        new TipoEntreno { Nombre = "Regenerativo", Color = "#32CD32" },
        new TipoEntreno { Nombre = "Otro", Color = "#FFFFFF" }
    };

    protected override async Task OnInitializedAsync()
    {
        await CargarCalendario();
    }

    private async Task CargarCalendario()
    {
        diasEnMes = DateTime.DaysInMonth(fechaActual.Year, fechaActual.Month);
        var primerDiaMes = new DateTime(fechaActual.Year, fechaActual.Month, 1);
        diasVaciosInicio = (int)primerDiaMes.DayOfWeek;

        try
        {
            logsDelMes = await Http.GetFromJsonAsync<List<RunningLogs>>($"https://apitcb.pladse.dsistemas.educacionchiapas.gob.mx/api/runningapi/plan/{TargetUserId}/{fechaActual.Month}/{fechaActual.Year}");
        }
        catch { logsDelMes = new List<RunningLogs>(); }
    }

    private RunningLogs ObtenerLog(int dia) => logsDelMes.FirstOrDefault(l => l.Fecha.Day == dia);

    private string ObtenerColor(string tipo)
    {
        var t = TiposEntreno.FirstOrDefault(x => x.Nombre == tipo);
        return t != null ? t.Color : "#F8C545";
    }

    // --- NAVEGACIÓN MESES ---
    private async Task MesAnterior() { fechaActual = fechaActual.AddMonths(-1); await CargarCalendario(); }
    private async Task MesSiguiente() { fechaActual = fechaActual.AddMonths(1); await CargarCalendario(); }

    // --- MODAL ---
    private void AbrirModal(int dia)
    {
        fechaSeleccionada = new DateTime(fechaActual.Year, fechaActual.Month, dia);
        var existente = ObtenerLog(dia);

        if (existente != null)
        {
            // EDICIÓN: Copiamos todo, incluyendo el feedback del atleta
            logEditando = new RunningLogs
            {
                RunningLogID = existente.RunningLogID,
                UsuarioID = existente.UsuarioID,
                Fecha = existente.Fecha,
                Titulo = existente.Titulo,
                Descripcion = existente.Descripcion,
                Tipo = existente.Tipo,

                // Feedback
                Realizado = existente.Realizado,
                RPE = existente.RPE,
                ComentariosAtleta = existente.ComentariosAtleta
            };
        }
        else
        {
            // NUEVO
            logEditando = new RunningLogs
            {
                UsuarioID = TargetUserId,
                Fecha = fechaSeleccionada,
                Tipo = "Acumulativo",
                Titulo = "",
                Descripcion = ""
            };
        }
        mostrarModal = true;
    }

    private void SeleccionarTipo(string tipo) => logEditando.Tipo = tipo;

    private async Task GuardarLog()
    {
        if (string.IsNullOrEmpty(logEditando.Titulo))
        {
            await App.Current.MainPage.DisplayAlert("Falta Título", "Pon una instrucción breve", "OK");
            return;
        }
        if (logEditando.Descripcion == null) logEditando.Descripcion = "";

        try
        {
            var resp = await Http.PostAsJsonAsync("https://apitcb.pladse.dsistemas.educacionchiapas.gob.mx/api/runningapi/guardar", logEditando);
            if (resp.IsSuccessStatusCode)
            {
                mostrarModal = false;
                await CargarCalendario();
            }
            else
            {
                var error = await resp.Content.ReadAsStringAsync();
                await App.Current.MainPage.DisplayAlert("Error", $"API: {error}", "OK");
            }
        }
        catch (Exception ex) { await App.Current.MainPage.DisplayAlert("Error", ex.Message, "OK"); }
    }

    private async Task EliminarLog()
    {
        bool confirm = await App.Current.MainPage.DisplayAlert("Borrar", "¿Quitar este entrenamiento?", "Sí", "No");
        if (!confirm) return;

        try
        {
            var resp = await Http.PostAsync($"https://apitcb.pladse.dsistemas.educacionchiapas.gob.mx/api/runningapi/eliminar/{logEditando.RunningLogID}", null);
            if (resp.IsSuccessStatusCode)
            {
                mostrarModal = false;
                await CargarCalendario();
            }
        }
        catch { }
    }

    // Método para colores RPE
    private string ObtenerColorRPE(int valor)
    {
        if (valor <= 2) return "#32CD32"; // Verde
        if (valor <= 4) return "#ADFF2F"; // Limón
        if (valor <= 6) return "#FFD700"; // Amarillo
        if (valor <= 8) return "#FF8C00"; // Naranja
        return "#FF0000"; // Rojo
    }

    private void CerrarModal() => mostrarModal = false;
    private void Volver() => Nav.NavigateTo("/usuarios-view");
}